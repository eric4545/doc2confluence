# Skip Cypress binary download (not used in this project)
# Cypress is a transitive dependency from @atlaskit packages
CYPRESS_INSTALL_BINARY=0

# Prefer offline to speed up installs
prefer-offline=true
